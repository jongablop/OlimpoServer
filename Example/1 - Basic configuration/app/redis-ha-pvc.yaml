apiVersion: v1
kind: PersistentVolumeClaim
metadata:
        name: redis-app-redis-ha-server-0
        namespace: app
        labels:
                app: redis
                component: master
                heritage: Helm
                role: master
                release: redis-app
spec:
        #storageClassName: manual
        accessModes:
                - ReadWriteOnce
        resources:
                requests:
                        storage: 8Gi

